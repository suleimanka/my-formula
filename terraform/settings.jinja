{%- set arch_map = {
      'x86_64': 'amd64',
      'aarch64': 'arm64',
    } %}
{%- set terraform_default = {
      'url': 'https://releases.hashicorp.com/terraform',
      'versions': ['1.8.5'],
      'default': '1.8.5',
      'arch': arch_map.get(grains.get('osarch', grains.get('cpuarch')), grains.get('cpuarch')),
      'os': 'linux',
    } %}
{%- set terraform = salt['pillar.get']('terraform', default=terraform_default, merge=True) %}
